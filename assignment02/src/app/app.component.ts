import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'assignment02';
  note = 'Attributes and properties are not the same .Attributes - HTML . Properties - DOM Attributes cannot change once they are initilize . However properties can change.'
}
